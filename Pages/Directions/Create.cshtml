@page
@model ContosoUniversity.Pages.Directions.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Direction</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Direction.DirectionName" class="control-label"></label>
                <input asp-for="Direction.DirectionName" class="form-control" />
                <span asp-validation-for="Direction.DirectionName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Direction.StudentIdent" class="control-label"></label>
                <input asp-for="Direction.StudentIdent" class="form-control" />
                <span asp-validation-for="Direction.StudentIdent" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
