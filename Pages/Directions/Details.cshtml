@page
@model ContosoUniversity.Pages.Directions.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Direction</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Direction.DirectionName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Direction.DirectionName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Direction.StudentIdent)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Direction.StudentIdent)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Direction.ID">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
